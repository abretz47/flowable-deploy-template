Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm get values {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

To see deployment status, try:

  $ kubectl get pods -n {{ .Release.Namespace }}

Flowable Work:
{{ index .Values.flowable.ingress.host }}/work

{{- if eq "dev" .Release.Namespace }}
Flowable Design:
{{ index .Values.flowable.ingress.host }}/design
{{- end }}

Flowable Control:
{{ index .Values.flowable.ingress.host }}/control